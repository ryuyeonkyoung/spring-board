# 모든 환경에 공통 적용되는 기본값
# 공통 설정 + active profile 설정

server:
  port: 8000

spring:
  profiles:
    active: local  # 기본값은 local로 설정 (개발자용)

  thymeleaf:
    cache: false

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false